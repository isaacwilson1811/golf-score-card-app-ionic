import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Golf Score Card App Ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
